# Copiar a .env para correr el backend fuera de Docker (uvicorn directo).
# .env esta en .gitignore — nunca se commitea.

# Local con docker compose:
SQL_CONNECTION_STRING=Server=tcp:localhost,1433;Database=master;User ID=sa;Password=Obra493$Local;Encrypt=false;

# Contra la base real de Azure (la password la imprime deploy-obra493.ps1):
# SQL_CONNECTION_STRING=Server=tcp:sql-obra493.database.windows.net,1433;Database=db-obra493;User ID=obra493admin;Password=...;Encrypt=true;

CORS_ORIGINS=http://localhost:5173
STORAGE_ACCOUNT=devstoreaccount1
STORAGE_CONTAINER=documentos
